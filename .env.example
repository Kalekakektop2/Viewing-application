# --- Production beta: leave GEMINI_API_KEY empty ---
# Viewing uses public backend (key only on Netlify):
# VIEWING_BACKEND_URL=https://symphonious-cobbler-d99370.netlify.app/.netlify/functions/analyze

# --- Local dev only (optional direct Gemini, never ship key in player builds) ---
# GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-flash-latest
VIEWING_BACKEND_URL=https://symphonious-cobbler-d99370.netlify.app/.netlify/functions/analyze
