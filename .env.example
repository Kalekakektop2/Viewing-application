# --- Production beta: leave GEMINI_API_KEY empty ---
# Viewing uses public backend (key only on Vercel/Netlify):
VIEWING_BACKEND_URL=https://game-vision-site.vercel.app/api/analyze

# --- Local dev only (optional direct Gemini, never ship key in player builds) ---
# GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-flash-latest
